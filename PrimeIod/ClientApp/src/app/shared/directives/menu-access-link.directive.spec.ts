import { MenuAccessLinkDirective } from './menu-access-link.directive';

describe('MenuAccessLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuAccessLinkDirective();
    expect(directive).toBeTruthy();
  });
});
