Command executed:
ng g clar-collection:list-edit --name TblTaskCategory 

Post actions:
npm run build

todo: 1. a, b.
--------------------------------------------------
import TblTaskCategoryModule into a parent Module
  example imports TblTaskCategoryModule into MasterModule:
  
//todo: a. 
 //*****************************
 import {TblTaskCategoryModule} from '@app/src/app/masters/tbl-task-category/tbl-task-category/tbl-task-category.module'
 
 @NgModule({
   imports: [
     ...
     //todo: b.
     //*****************************
     TblTaskCategoryModule
   ],
  
 })
 export class MastersModule {}
---------------------------------------------------

todo: 2. a, b
connect  TblTaskCategoryListComponent to a routing module:
   example connects TblTaskCategoryListComponent to MastersRoutingModule: 
   
   import { NgModule } from '@angular/core';
   import { Routes, RoutersModule } from '@angular/router';
  //todo: a
  //*****************************

  import { TblTaskCategoryListComponent} from './tbl-task-category/tbl-task-category-list/tbl-task-category-list.component'

  todo:b insert:
     //*****************************
     { path: 'tbl-task-category', component: TblTaskCategoryListComponent },
     . . .
     { path: '**', redirectTo: '', pathMatch: 'full' }
   ];
  
   @NgModule({
     imports: [RouterModule.forChild(routes)],
     exports: [RouterModule]
   })
   export class MastersRoutingModule {}
  

todo 3: 
Add menu item:
Example adds the menu item in header.component.html:
insert in:
ClientApp\src\app\shell\header\header.component.html

  <button
    class="dropdown-item"
    routerLink="/masters/tbl-task-category"
    routerLinkActive="active"
    translate
    *appHasPermissions="'TblTaskCategory.view'"
  >
    TblTaskCategory
  </button> 

todo 4:
Run the application 
  and add the permissions to the logged in test user's role
  and test.

todo 5 : Optional
Convert TblTaskCategory.view permission to
 tbl-task-category.view
Note: this action is optional and is required only to maintain consistency in naming conventions.
  The Dotnet Core generator (in the current version) is not able to generate a dasherized version of TblTaskCategory
  and the angular schematic is not above to generate a lower case version of TblTaskCategory.
  Therefore both the generatos produce TblTaskCategory.view . 
  Recomended to change 
    TblTaskCategory.view to tbl-task-category.view
    TblTaskCategory.modify to tbl-task-category.modify
    Ensure that corresponding changes are made in
      DAL\Core\ApplicationPermissions\partials\TblTaskCategory-ApplicationPermissions.cs

