import { StudyMenuAccessLinkDirective } from './study-menu-access-link.directive';

describe('StudyMenuAccessDirective', () => {
  it('should create an instance', () => {
    const directive = new StudyMenuAccessLinkDirective();
    expect(directive).toBeTruthy();
  });
});
