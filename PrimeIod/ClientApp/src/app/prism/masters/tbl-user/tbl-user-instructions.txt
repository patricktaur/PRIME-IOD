Command executed:
ng g clar-collection:list-edit --name TblUser 

Post actions:
npm run build

todo: 1. a, b.
--------------------------------------------------
import TblUserModule into a parent Module
  example imports TblUserModule into MasterModule:
  
//todo: a. 
 //*****************************
 import {TblUserModule} from '@app/masters/tbl-user/tbl-user.module';
 
 @NgModule({
   imports: [
     ...
     //todo: b.
     //*****************************
     TblUserModule
   ],
  
 })
 export class MastersModule {}
---------------------------------------------------

todo: 2. a, b
connect  TblUserListComponent to a routing module:
   example connects TblUserListComponent to MastersRoutingModule: 
   
   import { NgModule } from '@angular/core';
   import { Routes, RoutersModule } from '@angular/router';
  //todo: a
  //*****************************

  import {TblUserListComponent} from '@app/masters/tbl-user/tbl-user-list/tbl-user-list.component';

  todo:b insert:
     //*****************************
     { path: 'tbl-user', component: TblUserListComponent },
     . . .
     { path: '**', redirectTo: '', pathMatch: 'full' }
   ];
  
   @NgModule({
     imports: [RouterModule.forChild(routes)],
     exports: [RouterModule]
   })
   export class MastersRoutingModule {}
  

todo 3: 
Add menu item:
Example adds the menu item in header.component.html:
insert in:
app\shell\header\header.component.html

add lazy loaded module path if required:
routerLink="/lazy-loaded-module-path/tbl-user"
//******************************
  <button
    class="dropdown-item"

    routerLink="/masters/tbl-user"
    routerLinkActive="active"
    translate
    *appHasPermissions="'TblUser.view'"
  >
    TblUser
  </button> 
//****************************
todo 4:
Run the application 
  and add the permissions to the logged in test user's role
  and test.

todo 5 : Optional
Convert TblUser.view permission to
 tbl-user.view
Note: this action is optional and is required only to maintain consistency in naming conventions.
  The Dotnet Core generator (in the current version) is not able to generate a dasherized version of TblUser
  and the angular schematic is not above to generate a lower case version of TblUser.
  Therefore both the generatos produce TblUser.view . 
  Recomended to change 
    TblUser.view to tbl-user.view
    TblUser.modify to tbl-user.modify
    Ensure that corresponding changes are made in
      DAL\Core\ApplicationPermissions\partials\TblUser-ApplicationPermissions.cs

